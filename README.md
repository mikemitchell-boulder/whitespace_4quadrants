README — Whitespace Analysis Deliverables (v6)

Medians (fixed for consistency):
• X-axis (Total Whitespace ARR): 140,954
• Y-axis (Current ARR): 70,682


Contents:
01_SalesRep_Charts/       → Interactive HTML (one per rep + org)
02_CompanyType_Charts/    → Interactive HTML (one per type)
03_Summary_Tables/        → Counts + % by quadrant (rep & type) + combined workbook
04_Presentation/          → PPTX with org + rep slides and HTML links
05_PNGs/                  → Slide-ready PNGs (1920×1080) for reps and org

Notes:
• Points are black; labels appear on hover in HTML.
• HTML links will open in your default browser when clicked from the PPT.
